{
  "version": "2.9.2",
  "code": 49,
  "versionTag": "v2.9.2",
  "log": "\n### v2.9.2 — 修复「国内用户检测不到新版本」终极修复\n\n#### 一、 自动更新系统国内网络全链路修复\n1. **新增 3 个国内可直连的 release.json 代理源（永久解决）**\n   - `GhProxyRawSource`：通过 gh-proxy.com 代理获取 GitHub Raw 实时内容，绕过 GFW 封锁\n   - `GhFastRawSource`：通过 ghfast.top CDN 代理，独立线路冗余\n   - `GhProxyOrgRawSource`：通过 gh-proxy.org 代理，第三重保障\n   - 国内探测顺序优化：API → 3×代理源 → Gitee → GitHub Raw → JSDelivr，确保**每次都能拿到最新版本号**，不再受 jsDelivr @main 分支 7 天缓存影响\n2. **配套新增 URL 构造器**\n   - `UpdateConfig.ghProxyRawUrl() / ghFastRawUrl() / ghProxyOrgRawUrl()`\n3. **jsDelivr CDN 缓存已实时 purge 刷新**\n   - 当前 cdn.jsdelivr.net/gh/.../main/release.json 已返回 v2.9.2\n\n#### 二、 本地 APK 与 release.json 一致性修复\n- 修复 `/workspace/release/taskflow-*.apk` 复制错误导致的 sha256/size 与 release.json 不匹配问题\n- 新 APK 所有产物：build/outputs ↔ artifacts/ ↔ release/ 三目录哈希一致\n\n#### 三、 老用户立即生效说明（无需先安装 v2.9.2）\n- 即使用户仍停留在 v2.8.0 / v2.9.0 / v2.9.1，只要在 App 内点击「检查更新」，**现在就可以检测到 v2.9.2**\n- 原理：jsDelivr CDN 缓存已即时刷新返回 v2.9.2 → 老版本里原有的 JSDelivrSource 就能拿到最新版本号\n- 安装完 v2.9.2 后，以后的 v2.9.3 / v3.0.0 更新会通过 3 个国内代理源秒级检测，不再等 CDN 缓存\n\n---\n\n### v2.9.1 — 商业级 UI/UX 精修 + 番茄专注深度重构 + 桌面小组件交互统一（历史）\n\n#### 一、 全局商业级 UI/UX 美化大改造\n1. **状态栏沉浸修复 (顶栏白块 Bug)**\n   - `PomodoroScreen` 壁纸层重构为全屏背景（置于 Scaffold 背后，fillMaxSize 无 padding），渐变/图片直接延伸至屏幕最顶端，彻底消除顶部白色遮挡。\n2. **应用图标 & 启动页**\n   - 沿用 Android 12+ `SplashScreen` API + 矢量自适应图标 (Adaptive Icon)，紫蓝渐变背景 + 勾选框/Flow 圆点前景 + monochrome 单色层。\n3. **全屏闹钟 (AlarmActivity) 重构**\n   - 高质感强弹界面：全屏深紫渐变背景 + 径向呼吸光晕动画（2.2s 呼吸周期）+ ⏰ 脉冲缩放动画（0.9s）。\n   - 高饱和度圆角渐变按钮：全部完成（翡翠绿）、稍后提醒（暗玻璃描边）、关闭闹钟（紫红渐变）。\n\n#### 二、 任务表单逻辑修补\n1. **暂停限制时长配置 (新增)**\n   - `TaskEntity` 新增 `pauseLimitMinutes`，默认 2 分钟。\n   - 表单提供 FilterChips：`[1分钟] [2分钟(默认)] [5分钟] [⚙️自定义]`，自定义弹窗 1~30 分钟校验。\n\n#### 三、 桌面小组件交互统一\n1. **左侧圆圈 Checkbox**：点击直接完成/恢复任务，不跳转页面，并刷新小组件（`ACTION_TOGGLE_TASK` + `WidgetHelper.refresh`）。\n2. **卡片主体 (Card Body)**：点击带参 `taskId` 直接进入【番茄专注模式 (PomodoroScreen)】（新增 `ACTION_OPEN_POMODORO` 路由，MainActivity → TaskFlowNavHost）。\n\n#### 四、 番茄专注模式深度开发\n1. **进页自动倒计时 (Auto-Start)**：从主页/小组件点击卡片主体进入即自动开启专注，无需手动点击播放。\n2. **暂停限制时间弹窗 (Pause Limit Timer)**：倒计时中点击暂停立即弹模态倒计时（读取任务 `pauseLimitMinutes`，如 02:00），归零后自动继续专注；提示文案「以上是暂停限制时间，避免过长打断专注流程」+ `[▶ 继续专注]` 按钮。\n3. **自定义背景音乐 (Custom Audio Manager)**：音乐弹窗增加 `[💾 本地预置] [🌐 在线流媒体] [📁 自定义导入]`，调用 `ActivityResultLauncher` 读取手机本地 MP3 音频（`AudioSource.LocalFile`，takePersistableUriPermission）。\n4. **自定义背景图片/壁纸 (Wallpaper Selector)**：`[🖼️ 壁纸设置]` BottomSheet 分类：`[风景] [动物] [极简/杂项]` + `➕ 自定义上传`（相册选择，Coil 渲染 + 高斯遮罩）。\n5. **屏幕常亮开关**：`[☀️ 屏幕常亮]` 绑定 `WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON`，退出页面自动清除。\n\n#### 构建信息\n- versionCode: 49\n- versionName: 2.9.2\n\n---\n\n",
  "apk": "https://github.com/Zero-william163/TaskFlow-Pro/releases/download/v2.9.2/taskflow-v2.9.2.apk",
  "sha256": "8db0ed02bd850fed163c4461d6b4ccfd10542acacc090c2042cc2235ccb43957",
  "size": 46868687,
  "downloadUrls": [
    {
      "name": "GH Proxy",
      "url": "https://gh-proxy.com/https://github.com/Zero-william163/TaskFlow-Pro/releases/download/v2.9.2/taskflow-v2.9.2.apk",
      "region": "domestic"
    },
    {
      "name": "GH Fast",
      "url": "https://ghfast.top/https://github.com/Zero-william163/TaskFlow-Pro/releases/download/v2.9.2/taskflow-v2.9.2.apk",
      "region": "cdn"
    },
    {
      "name": "GH Proxy Org",
      "url": "https://gh-proxy.org/https://github.com/Zero-william163/TaskFlow-Pro/releases/download/v2.9.2/taskflow-v2.9.2.apk",
      "region": "domestic"
    },
    {
      "name": "GitHub",
      "url": "https://github.com/Zero-william163/TaskFlow-Pro/releases/download/v2.9.2/taskflow-v2.9.2.apk",
      "region": "international"
    }
  ]
}
